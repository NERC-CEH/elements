# Remove the filehash database connection from the global environment

Remove the filehash database connection from the global environment

## Usage

``` r
shutdown()
```

## Examples

``` r
elements::shutdown()
```
